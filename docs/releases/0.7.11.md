# Cassette Motion Pro v0.7.11

## Installer artifact fix

This update fixes the GitHub Actions installer artifact.

### Changes

- Adds the `.github/workflows/build.yml` workflow to the upload package.
- Updates the workflow version to `0.7.11`.
- Runs NSIS from inside `src\Installer` so the installer `.exe` is created where the upload step expects it.
- Updates the NSIS installer version to `0.7.11`.
- Keeps the v0.7.10 Before/After video side-by-side fix.

### Expected GitHub Actions artifacts

After a successful run, you should see:

- `CassetteMotionPro-Portable-0.7.11`
- `CassetteMotionPro-Installer-0.7.11`
